/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'server.halcyonbd.com',
      'via.placeholder.com',
      'lh3.googleusercontent.com',
    ],
  },
};

module.exports = nextConfig;
